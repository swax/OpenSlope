diff --git a/src/libsatsuma/Reductions/BiMDF_to_BiMCF.cc b/src/libsatsuma/Reductions/BiMDF_to_BiMCF.cc
index a0772ba..dfb372a 100644
--- a/src/libsatsuma/Reductions/BiMDF_to_BiMCF.cc
+++ b/src/libsatsuma/Reductions/BiMDF_to_BiMCF.cc
@@ -153,7 +153,9 @@ BiMDF_to_BiMCF::BiMDF_to_BiMCF(const BiMDF &_bimdf, Config const& _config)
             if (cost < 0 ) { cost = 0;}  // we never want an unbounded arc with negative costs!
             int remain = upper;
             if (upper < BiMCF::inf()) {
-                remain -= dev;
+                // capacity remaining above the deviation arcs already added; using plain
+                // `upper - dev` would let the flow exceed a finite upper bound.
+                remain = upper - guess - dev;
             }
             add_edge(mdf_edge, true, cost, remain);
         }
diff --git a/src/libsatsuma/Solvers/EvenBiMDF.cc b/src/libsatsuma/Solvers/EvenBiMDF.cc
index 0ce9412..0a838cc 100644
--- a/src/libsatsuma/Solvers/EvenBiMDF.cc
+++ b/src/libsatsuma/Solvers/EvenBiMDF.cc
@@ -97,9 +97,11 @@ EveningResult TJoinBasedRounding::solve()
     for (const auto e: g.edges())
     {
         //assert(bimdf_.upper[e] == BiMDF::inf());
-        if (bimdf_.upper[e] <= bimdf_.lower[e]) {
-            throw std::runtime_error("BiMDF has arc with upper<= lower, please remove.");
+        if (bimdf_.upper[e] < bimdf_.lower[e]) {
+            throw std::runtime_error("BiMDF has arc with upper < lower, please remove.");
         }
+        // NB: upper == lower (a fixed arc) is allowed: it offers no +-1 adjustment, so its
+        // cost_ stays infinite and the T-join must route parity corrections around it.
         // TODO: we might round towards an odd bound, which is later rounded towards guess! is this a problem?
         // TODO: use find_best_adjustment
         auto lower = bimdf_.lower[e];
@@ -109,6 +111,9 @@ EveningResult TJoinBasedRounding::solve()
         if (rounded < lower) {
             rounded = lower;
         }
+        if (rounded > upper) {
+            rounded = upper;
+        }
         guess[e] = rounded;
         auto base_cost = bimdf_.cost(e, rounded);
         cost_[e] = std::numeric_limits<double>::infinity();
@@ -149,6 +154,10 @@ EveningResult TJoinBasedRounding::solve()
     size_t n_adjustments = 0;
     for (const auto e: g.edges()) {
         if (sol[e]) {
+            if (!std::isfinite(cost_[e])) {
+                throw InternalError("T-join parity correction selected an arc with no legal adjustment"
+                                    " (probably lower == upper): prescription is parity-infeasible here.");
+            }
             guess[e] = adjusted_guess_[e];
             auto opti = std::llround(bimdf_.guess(e));
             cost += bimdf_.cost(e, guess[e]) - bimdf_.cost(e, opti);
